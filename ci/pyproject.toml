[project]
name = "spellbook-ci"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["dagger-io>=0.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/spellbook_ci"]
